services:
  mongo-database:
      volumes: 
        - data:/data/db #use data for volume which is specified below
      networks: 
        - internal-network


volumes: 
  data:  #this is the name that is referenced above, leave as data
    driver: local
    name: example.volume #put the namme of the volume
